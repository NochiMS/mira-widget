<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/widget_bg"
    android:paddingStart="16dp"
    android:paddingEnd="16dp">

    <!-- Gelişim dönemi emojisi — Kotlin tarafından güncellenir -->
    <TextView
        android:id="@+id/tv_emoji"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🌸"
        android:textSize="24sp"/>

    <!-- Mira — sabit, altın renk -->
    <TextView
        android:id="@+id/tv_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mira"
        android:textColor="#e8c87a"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"/>

    <!-- İnce ayırıcı çizgi -->
    <View
        android:layout_width="1dp"
        android:layout_height="20dp"
        android:background="#44e8c87a"
        android:layout_marginEnd="10dp"/>

    <!-- Yaş: Xy  Xa  Xg — Kotlin tarafından güncellenir -->
    <TextView
        android:id="@+id/tv_age"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="0y  0a  27g"
        android:textColor="#f0ebe0"
        android:textSize="13sp"/>

</LinearLayout>
